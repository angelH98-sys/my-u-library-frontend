import { error } from "./error/error.translations";
import { form } from "./form/form.translations";
import { ui } from "./ui/ui.translations";

export const en = {
  translation: {
    ui,
    form,
    error,
  }
}